# 数据源
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.221.128:3306/gulimall_sms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gulimall-coupon

# mapper映射位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      # id主键自增
      id-type: auto

